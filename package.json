{
  "name": "sih2026-rbcet-team-register",
  "version": "1.0.0",
  "private": true,
  "description": "SIH 2026 RBCET internal round - registration and participant management, backed by Postgres",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "db:setup": "node src/setup-db.js",
    "db:reset": "node src/setup-db.js --force",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "pg": "^8.16.3"
  }
}
